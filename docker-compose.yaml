version: '3.8'

services:
  playwright-test:
    
    build:
      context: .
      dockerfile: ./Dockerfile
    command: npm run product:prod
    volumes:
      - ./playwright-report/:/app/playwright-report
      - ./test-results/:/app/test-results
